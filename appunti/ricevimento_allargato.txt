COSE DA CHIDERE:
- dato che abbiamo l'accesso con email e password creiamo un bypass per user user

  SI BYPASS

- per le persone non vedenti o comunque per chiarezza quando un utente cerca di esntrare in una 
  pagina dove è necessario essre loggati come gestire il reindirizzamento? direttamente pagina o 
  pagina intermedia di "avviso"?

  Se c'è il redirect avviso di reindirizzamento che compare 

- per la questioni di estensione immagini jpg per web e png per stampa dato che le immagini di contenuto 
  sono caricate dagli utenti si pensava a una funzone (GD o js) per traformarle in jpg, ma richidere 
  permessi di modifica file e molto spesso vine bypassato + figure con png per stampa e jpg per web

  o consentito solo jpg o conversione o così

- diversi account con i quali fare query

  NON SI PUO' FARE
  
DA FARE:
- profilo visualizzabile da esterni all'utente
- modifica annuncio
- vaffancuo
- esplora gestito anche tramite js
- ul della navbar non deve essere duplicato

Da total validator:
v - mettere aria-label contenenti l'espressione in label visibile
v - mettere testo alternativo #n

contatti sotto
asterisco per richiesto
no attenzione
torna su sticky 
conferma per elimina

#hamburger-menu {
    display: none;
    width: 100%;
    padding-top: 0.5em;
    box-shadow: 0 -0.5em 0.5em -0.5em rgba(0,0,0,0.3); 
}

#hamburger-menu.attivo{
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.8em;
    margin-bottom: 1.2em;
}

#hamburger-menu .nav-links, 
#hamburger-menu .nav-auth {
    list-style: none;
    display: flex;
    margin: 0;
    padding: 0;
    flex-direction: column;
}

#hamburger-menu .nav-links{
    gap: var(--gap-1);
}

#hamburger-menu .nav-auth {
    gap: var(--gap-1-5);
}

#hamburger-menu a {
    color: var(--txtcolor);
    padding: var(--padding-base);
}

#hamburger-menu .nav-links a{
    text-decoration: underline;
    text-underline-offset: 0.2em;
}

#hamburger-menu a:visited {
    text-decoration: underline;
    text-underline-offset: 0.2em;
    color: var(--visited);
}

#hamburger-menu .link.call-to-action{
    color: var(--clearTxtColor);
}

#hamburger-menu .link.call-to-action:visited{    
    color: var(--call-to-actionColor);
    background-color: var(--BgColor);
}

#hamburger-menu .link.call-to-action:hover,
#hamburger-menu .link.call-to-action:focus{
    background-color: var(--call-to-actionAction);
    color: var(--call-to-actionColor);
    outline: var(--btn-border);
    outline-offset: 1px;
}

#hamburger-aperto.attivo,
#hamburger-chiuso.attivo {
    display: flex;
}